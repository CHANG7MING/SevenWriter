# SevenWriter 核心质量案例集

本目录评估写作质量，不与 `../smoke/` 的程序冒烟测试混用。案例必须提供完整任务语境、可审计约束和人工验收标准；只有通过 `validate_quality_cases.py` 的案例才计入覆盖率。

每个一级场景至少包含一条核心案例。后续扩展目标是交叉覆盖四种任务（创作、诊断、轻改、深改）与四种稿件状态（优质、普通、问题、受保护），但不机械制造 23×4×4 个低质量组合。

`excellent` 案例的核心指标是克制：没有明确收益时应保持原稿，不得为了提高分数而重写。
